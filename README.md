# weddingcountdown

Static wedding countdown site.

## Run
- Open `index.html` in your browser.

## Background photo
- Put your photo in `assets/` as `background.jpg` (or `.jpeg/.png/.webp`), or keep using the Google Drive fallback in `script.js`.

